import React from "react";

const footer = (props) => {
    return (
        <footer className="bg-dark text-center text-white p-2">
            H2PROG - Tout droits réservés.
        </footer>
    )};

export default footer;